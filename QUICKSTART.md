# Быстрый старт Password Manager

## 1️⃣ Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd passwordmanager

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или на Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

## 2️⃣ Конфигурация

```bash
# Скопируйте .env.example в .env
cp .env.example .env

# Отредактируйте .env и добавьте ваш Telegram Bot Token
# TELEGRAM_BOT_TOKEN=your_token_here
```

Как получить токен:
1. Откройте Telegram и найдите @BotFather
2. Выполните команду `/start`
3. Выполните `/newbot` и следуйте инструкциям
4. Скопируйте полученный токен в `.env`

## 3️⃣ Запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустите бота
python3 main.py
```

Вы должны увидеть:
```
INFO:root:Database initialized successfully
INFO:root:Starting bot...
```

## 4️⃣ Тестирование

```bash
# Установите зависимости разработки
pip install -r requirements-dev.txt

# Запустите тесты
pytest tests/ -v

# Проверьте код
flake8 src/ tests/
mypy src/
```

## 🤖 Использование бота

1. Откройте Telegram и найдите вашего бота
2. Нажмите `/start`
3. Выберите "Регистрация" или "Вход"

### Основные команды

- **/start** - Перезагрузить главное меню
- **📝 Регистрация** - Создать новый аккаунт
- **🔑 Вход** - Вход в существующий аккаунт
- **➕ Добавить пароль** - Сохранить пароль для сервиса
- **👁️ Просмотр паролей** - Посмотреть все пароли
- **✏️ Обновить пароль** - Изменить существующий пароль
- **🗑️ Удалить пароль** - Удалить пароль
- **🚪 Выход** - Выход из аккаунта

## 📁 Структура файлов

```
src/
├── bot/           - Telegram бот (handlers, states, keyboards)
├── database/      - SQLite (models, CRUD операции)
├── config.py      - Конфигурация
└── utils.py       - Вспомогательные функции

tests/            - Unit тесты

main.py           - Точка входа приложения
```

## 🔧 Разработка

### Добавление новой функции

1. Добавьте обработчик в `src/bot/handlers.py`
2. Добавьте состояние в `src/bot/states.py` (если нужно)
3. Напишите тесты в `tests/`
4. Запустите `pytest tests/ -v`

### Структура обработчика

```python
@router.message(MainMenuStates.MENU, F.text == BTN_YOUR_ACTION)
async def your_handler(message: Message, state: FSMContext):
    # Ваша логика здесь
    await message.answer("Ответ пользователю")
    await state.set_state(NextState.STATE)
```

## ⚠️ Важно

- Пароли хранятся **в открытом виде** в SQLite БД
- Для production необходимо добавить шифрование
- Не коммитьте `.env` файл в Git

## 📚 Дополнительно

Подробную документацию см. в [README.md](README.md)
