# Contributing to Password Manager

Спасибо за интерес к Password Manager! Мы приветствуем вклады от всех.

## Как начать разработку

### 1. Подготовка окружения

```bash
# Форкируйте репозиторий и клонируйте его
git clone https://github.com/your-username/passwordmanager.git
cd passwordmanager

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости для разработки
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

### 2. Создайте ветку для вашего вклада

```bash
# Создайте новую ветку
git checkout -b feature/your-feature-name
# или для bug fixes
git checkout -b fix/bug-description
```

## Правила разработки

### Code Style

Мы используем **PEP 8** и **Black** для форматирования кода.

```bash
# Форматирование кода
black src/ tests/

# Проверка стиля
flake8 src/ tests/

# Проверка типов
mypy src/
```

### Commits

- Используйте осмысленные сообщения коммитов на английском языке
- Первая строка - краткое описание (до 50 символов)
- Пустая строка, затем детальное описание (если необходимо)

Примеры:
```
Add password validation function

Implement email and username validation with regex patterns.
```

```
Fix user session timeout issue

Previously, sessions were not properly invalidated after logout.
Now using Redis to track active sessions.
```

## Тестирование

Все новые функции должны включать тесты.

```bash
# Запуск всех тестов
pytest tests/ -v

# Запуск конкретного теста
pytest tests/test_database.py::TestUserCRUD::test_create_user -v

# Проверка покрытия кода
pytest tests/ --cov=src --cov-report=html
```

### Написание тестов

```python
def test_my_feature():
    """Test description"""
    # Arrange
    expected = "result"
    
    # Act
    actual = my_function()
    
    # Assert
    assert actual == expected
```

## Pull Request Process

1. **Обновите вашу ветку** с последними изменениями из main:
```bash
git fetch origin
git rebase origin/main
```

2. **Запустите тесты локально**:
```bash
pytest tests/ -v
black src/ tests/ --check
flake8 src/ tests/
mypy src/
```

3. **Создайте Pull Request** на GitHub с описанием:
   - Что было сделано
   - Зачем это было сделано
   - Тип изменений (feature/fix/docs)

4. **Ответьте на комментарии** ревьюеров

## Типы вкладов

### 🎯 Features (Новые функции)

Примеры:
- Экспорт паролей в CSV
- Синхронизация паролей между устройствами
- Двухфакторная аутентификация
- Шифрование паролей

**Процесс**:
1. Обсудите идею в Issues
2. Создайте ветку `feature/your-feature`
3. Напишите код и тесты
4. Создайте PR

### 🐛 Bug Fixes (Исправления ошибок)

Примеры:
- Исправление ошибки удаления пароля
- Корректировка валидации email
- Исправление проблемы с сессией

**Процесс**:
1. Создайте Issue с описанием ошибки
2. Создайте ветку `fix/bug-description`
3. Напишите тест, воспроизводящий ошибку
4. Исправьте ошибку
5. Создайте PR

### 📚 Documentation (Документация)

Примеры:
- Улучшение README
- Добавление примеров использования
- Документация API

**Процесс**:
1. Создайте ветку `docs/your-changes`
2. Обновите документацию
3. Создайте PR

### 🧪 Tests (Тесты)

- Улучшение покрытия кода
- Добавление интеграционных тестов
- Оптимизация тестов

### ⚙️ Chores (Текущие задачи)

- Обновление зависимостей
- Рефакторинг кода
- Улучшение производительности

## Советы

### Перед тем как начать

- Проверьте существующие Issues и PRs
- Прочитайте [ARCHITECTURE.md](ARCHITECTURE.md)
- Запустите тесты локально

### Во время разработки

- Часто коммитьте с понятными сообщениями
- Пишите тесты параллельно с кодом
- Следуйте code style

### Перед отправкой PR

```bash
# Финальная проверка
pytest tests/ -v --cov=src
black src/ tests/
flake8 src/ tests/
mypy src/

# Убедитесь, что все тесты проходят
# Убедитесь, что нет lint ошибок
# Убедитесь, что код отформатирован
```

## Questions?

- Создайте [Issue](https://github.com/your-username/passwordmanager/issues)
- Обсудите в [Discussions](https://github.com/your-username/passwordmanager/discussions)

## License

Этот проект использует MIT License. Отправляя PR, вы соглашаетесь, что ваш вклад будет лицензирован под MIT License.

---

**Спасибо за ваш вклад!** 🎉
