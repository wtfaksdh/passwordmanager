# Password Manager - Telegram Bot

Менеджер паролей с интеграцией Telegram бота для управления паролями к различным сервисам.

## Функциональность

- 📝 **Регистрация и авторизация** - Создание аккаунта и вход в систему
- ➕ **Добавление паролей** - Сохранение паролей для различных сервисов
- 👁️ **Просмотр паролей** - Просмотр всех сохраненных паролей
- ✏️ **Обновление паролей** - Изменение существующих паролей
- 🗑️ **Удаление паролей** - Удаление ненужных паролей
- 🚪 **Выход** - Выход из аккаунта

## Структура проекта

```
passwordmanager/
├── src/
│   ├── __init__.py
│   ├── config.py                 # Конфигурация и константы
│   ├── utils.py                  # Утилиты и вспомогательные функции
│   ├── bot/
│   │   ├── __init__.py
│   │   ├── handlers.py           # Обработчики команд бота
│   │   ├── states.py             # Состояния FSM
│   │   └── keyboards.py          # Клавиатуры Telegram
│   └── database/
│       ├── __init__.py
│       ├── models.py             # Модели данных (User, Password)
│       ├── db.py                 # Инициализация БД SQLite
│       └── crud.py               # CRUD операции
├── tests/
│   ├── __init__.py
│   └── test_database.py          # Тесты CRUD операций
├── .github/workflows/
│   └── ci.yml                    # GitHub Actions CI/CD
├── main.py                       # Точка входа приложения
├── requirements.txt              # Зависимости проекта
├── requirements-dev.txt          # Зависимости для разработки
└── README.md                     # Этот файл
```

## Требования

- Python 3.9+
- SQLite3 (встроено в Python)
- aiogram 3.x (Telegram Bot API)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd passwordmanager
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

### Запуск бота

1. Установите переменную окружения с токеном Telegram:
```bash
export TELEGRAM_BOT_TOKEN="your_token_here"
```

2. Запустите приложение:
```bash
python main.py
```

### Использование в Telegram

1. Откройте бота в Telegram (@password_manager_secure_bot)
2. Нажмите `/start`
3. Выберите действие:
   - **📝 Регистрация** - Создайте новый аккаунт
   - **🔑 Вход** - Войдите в существующий аккаунт

После входа доступны:
- **➕ Добавить пароль** - Добавить новый пароль
- **👁️ Просмотр паролей** - Увидеть все пароли
- **✏️ Обновить пароль** - Изменить существующий пароль
- **🗑️ Удалить пароль** - Удалить пароль
- **🚪 Выход** - Выход из аккаунта

## Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements-dev.txt
```

### Запуск тестов

```bash
pytest tests/ -v
```

### Проверка кода

```bash
# Форматирование кода
black src/ tests/

# Статический анализ
flake8 src/ tests/

# Проверка типов
mypy src/
```

## CI/CD

Проект использует GitHub Actions для автоматического тестирования. Pipeline включает:
- Запуск тестов на Python 3.9, 3.10, 3.11, 3.12
- Проверку покрытия кода
- Линтинг (flake8)
- Проверку типов (mypy)

## База данных

Проект использует SQLite3 для хранения данных:

### Таблицы

**users**
- `id` - Уникальный идентификатор
- `username` - Имя пользователя (уникально)
- `password` - Пароль
- `created_at` - Дата создания
- `updated_at` - Дата обновления

**passwords**
- `id` - Уникальный идентификатор
- `user_id` - ID пользователя (внешний ключ)
- `service` - Название сервиса
- `login` - Логин/Email
- `password` - Пароль
- `created_at` - Дата создания
- `updated_at` - Дата обновления

## Безопасность

⚠️ **Примечание**: Пароли хранятся в открытом виде в SQLite БД. Для использования в production требуется:
- Добавить шифрование паролей
- Использовать защищенное соединение
- Добавить логирование доступа
- Регулярно создавать резервные копии

## API CRUD

### User CRUD

```python
from src.database.db import Database
from src.database.crud import UserCRUD
from src.database.models import User

# Создание пользователя
user = User(username="john", password="secret123")
user_id = UserCRUD.create(db, user)

# Поиск пользователя
user = UserCRUD.get_by_username(db, "john")
user = UserCRUD.get_by_id(db, user_id)

# Проверка пароля
user = UserCRUD.verify_password(db, "john", "secret123")

# Удаление пользователя
UserCRUD.delete(db, user_id)
```

### Password CRUD

```python
from src.database.db import Database
from src.database.crud import PasswordCRUD
from src.database.models import Password

# Создание пароля
pwd = Password(user_id=1, service="Gmail", login="user@gmail.com", password="secret")
pwd_id = PasswordCRUD.create(db, pwd)

# Получение паролей
pwd = PasswordCRUD.get_by_id(db, pwd_id)
passwords = PasswordCRUD.get_by_user_id(db, user_id)

# Обновление пароля
PasswordCRUD.update(db, pwd_id, password="newsecret")

# Удаление пароля
PasswordCRUD.delete(db, pwd_id)
```

## Лицензия

MIT License

## Контакты

Для вопросов и предложений создавайте Issues в репозитории.
